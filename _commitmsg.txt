Simplified panel add + "more settings" with fetched inbound/group/squad picker

Adding a panel now only asks: type (glass buttons) -> name -> base_url ->
credentials (user/pass or api_token). No inbound/squad/group during add.

New panel view -> "تنظیمات بیشتر":
- driver listTargets() fetches inbounds (3x-ui /panel/api/inbounds/list),
  groups (PasarGuard /api/groups) or internal squads (Remnawave
  /api/internal-squads); shown as selectable inline buttons.
  3x-ui = single inbound; Remnawave/PasarGuard = multi-select toggle.
- manual-entry fallback when the panel can't be reached.
- 3x-ui subscription host/port/path settings.

Endpoints verified against panel sources/SDKs; all wrapped in try/catch so a
fetch failure degrades to manual entry. 45 tests passing.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
